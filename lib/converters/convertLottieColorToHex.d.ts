import { HexColor, LottieColor } from '../types';
/**
 * Convert a lottie color to a hex color (without alpha channel).
 * @param lottieColor
 * @returns hex color
 */
export declare const convertLottieColorToHex: (lottieColor: LottieColor) => HexColor;
