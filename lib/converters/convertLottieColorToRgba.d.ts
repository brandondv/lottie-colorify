import { LottieColor, RgbaColor } from '../types';
/**
 * Convert a lottie color to an rgba color array.
 * @param lottieColor
 * @returns rgba color array
 */
export declare const convertLottieColorToRgba: (lottieColor: LottieColor) => RgbaColor;
